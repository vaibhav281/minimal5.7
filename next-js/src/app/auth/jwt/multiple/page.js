import { JwtMultipleLoginView } from 'src/sections/auth/jwt';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Jwt: multiple',
};

export default function LoginPage() {
  return <JwtMultipleLoginView />;
}
